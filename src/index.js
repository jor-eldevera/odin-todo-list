import "./style.css"

class Project {
    todoItems = [];

    constructor(title) {
        this.title = title;
    }
}

class TodoItem {
    constructor(title, description, dueDate, priority) {

    }
}